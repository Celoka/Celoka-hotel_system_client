import React from 'react';
import { NavBar } from './NavBar';
import Map from './Map';

const SearchBar = () => (
    <div className="search-box">
        <NavBar />
        <Map />
    </div>
)
export default SearchBar;
